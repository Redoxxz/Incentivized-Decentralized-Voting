Project By: Mohammad Ahmad

The program can be run by running the following command after correctly setting up the testing environment
I personally used Ganache which can be seen in the config file

npx hardhat run scripts/{TestFileName}.js --network localhost